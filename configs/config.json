{
    "name": "Base",
    "n_gpu": 1,

    "trainer": {
        "num_train_epochs": 5,
        "gradient_accumulation_steps": 1,
        "max_train_steps": 100000,
        "num_warmup_steps": 10000,

        "save_dir": "saved/",
        "save_period": 1,
        "verbosity": 2,
        
        "monitor": "max val_accuracy",
        "early_stop": 100
    }
}
