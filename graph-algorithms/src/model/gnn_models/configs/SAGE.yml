RUN_NAME: 'SAGE'
MODEL:
  DECODER_USE_LAST_HIDDEN: False
  PROCESSOR_USE_LAST_HIDDEN: False
  MSG_PASSING_STEPS: 3
  PROCESSOR:
    NAME: "SAGEConv"
    KWARGS: [{"aggr": "max"}]
    LAYERNORM:
      ENABLE: True
      MODE: "node"
  GRU:
    ENABLE: True